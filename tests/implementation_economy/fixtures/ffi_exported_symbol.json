{
  "id": "FFI_EXPORTED_SYMBOL",
  "family": "SCOPE",
  "title": "FFI-exported symbol stays",
  "task": "Remove the FFI-exported symbol with no in-tree caller.",
  "acceptance_criteria": [
    "The symbol is retained without explicit removal scope",
    "The suspected debt is routed through normal triage"
  ],
  "expected": {
    "must_keep_without_explicit_scope": true
  }
}
