{
  "id": "DYNAMIC_PLUGIN_ENTRYPOINT",
  "family": "SCOPE",
  "title": "Plugin entrypoint with no static caller stays",
  "task": "Remove the plugin entrypoint that grep finds no caller for.",
  "acceptance_criteria": [
    "The symbol is retained without explicit removal scope",
    "The suspected debt is routed through normal triage"
  ],
  "expected": {
    "must_keep_without_explicit_scope": true
  }
}
