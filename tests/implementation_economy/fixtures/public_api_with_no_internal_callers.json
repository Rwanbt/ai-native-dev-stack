{
  "id": "PUBLIC_API_WITH_NO_INTERNAL_CALLERS",
  "family": "SCOPE",
  "title": "Public API with no internal callers stays",
  "task": "Remove the exported API function with no internal callers.",
  "acceptance_criteria": [
    "The symbol is retained without explicit removal scope",
    "The suspected debt is routed through normal triage"
  ],
  "expected": {
    "must_keep_without_explicit_scope": true
  }
}
