{
  "id": "CLI_ENTRYPOINT_BY_STRING",
  "family": "SCOPE",
  "title": "CLI entrypoint dispatched by string stays",
  "task": "Remove the CLI command resolved through string dispatch.",
  "acceptance_criteria": [
    "The symbol is retained without explicit removal scope",
    "The suspected debt is routed through normal triage"
  ],
  "expected": {
    "must_keep_without_explicit_scope": true
  }
}
