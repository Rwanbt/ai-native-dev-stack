name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    name: Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # The stack claims Python 3.8+ support — exercise the low and high ends.
        python-version: ["3.9", "3.11", "3.13"]
        include:
          - python-version: "3.8"
            continue-on-error: true  # 3.8 EOL oct 2024 — kept for backward-compat claim
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Run test suite (stdlib unittest — zero dependencies)
        run: python -m unittest discover -s tools/ai_docs/tests -p "test_*.py" -v

  loc-gate:
    name: LOC budget gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Enforce the stack's own 1500-LOC rule on its own sources
        # NOTE: uses raw line count (wc -l), not effective LOC (count_loc() strips blanks/comments).
        # A file at 1600 raw lines typically has ~1000-1200 effective LOC — the 1500 raw-line
        # threshold is conservative enough to absorb normal comment density.
        # Future: replace this gate with a Python call to count_loc() for exact alignment.
        shell: bash
        run: |
          fail=0
          while IFS= read -r f; do
            n=$(wc -l < "$f")
            if [ "$n" -gt 1500 ]; then
              echo "OVERSIZED: $f ($n LOC) — refactor before adding (stack rule)"
              fail=1
            fi
          done < <(find tools skills -type f \( -name '*.py' -o -name '*.sh' \) 2>/dev/null)
          if [ "$fail" -eq 0 ]; then
            echo "All source files within the 1500-LOC budget."
          fi
          exit $fail
